{
  "name": "prompt-kickstart-mcp",
  "version": "1.0.0",
  "description": "Standalone MCP server: image_to_prompt + enhance_prompt (free, host-LLM vision)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "prompt-kickstart-mcp": "bin/prompt-kickstart-mcp.js"
  },
  "files": ["bin/", "dist/"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/prompts.test.ts test/enhance-prompt.test.ts test/image.test.ts test/image-to-prompt.test.ts test/storyboard-prompt.test.ts test/server.e2e.test.ts",
    "prepublishOnly": "npm run build && npm test"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "sharp": "^0.34.5",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
